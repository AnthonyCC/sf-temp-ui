{namespace pdp}

/**
 * @param productName
 * @param? akaName
 */
{template .productName}
	<h1>{if $productName}{$productName}{/if}
	{if $akaName}<p class="productDisplay-akaName">"{$akaName|noAutoescape}"</p>{/if}
  </h1>
{/template}
