{namespace pdp}

/**
 * @param productName
 * @param? akaName
 */
{template .productName}
	<h1 class="pdpTitle">{if $productName}{$productName}{/if}
	{if $akaName}<p class="productDisplay-akaName">"{$akaName|noAutoescape}"</p>{/if}
  <button class="annotation cssbutton transparent white icon-info2-before notext">?</button>
  </h1>
{/template}
