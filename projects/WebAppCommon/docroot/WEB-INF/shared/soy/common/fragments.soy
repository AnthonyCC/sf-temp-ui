{namespace common}


/**
 * @param scaleUnit
 * @param price
 * @param? wasPrice wasPrice
 * @param? aboutPriceText aboutPriceText
 */
{template .price autoescape="false"}
			{if $wasPrice}
				  <span class="save-price">${$price}/{$scaleUnit}</span> <s>(${$wasPrice})</s>
			{else}
				  ${$price}/{$scaleUnit}
			{/if}
			{if $aboutPriceText}
				<span class="about-price">{$aboutPriceText}</span>
			{/if}
{/template}
