{namespace common.rating}

/**
 * @param? ratingValue wine rating value
 */
{template .expert}
{if $ratingValue>0}
  <b class="expertrating smallrating rating-{$ratingValue*1}">
   Rating {$ratingValue}/10
  </b>
{/if}
{/template}
