{namespace common.rating}

/**
 * @param ratingValue wine rating value
 * @param reviewCount wine rating value
 */
{template .customer}
{if $ratingValue>0}
<b class="customerrating smallrating rating-{$ratingValue*1}" data-customer-rating="{$ratingValue/2}" data-customer-reviewcount="{$reviewCount}" >{$ratingValue/2}</b>
{/if}
{/template}