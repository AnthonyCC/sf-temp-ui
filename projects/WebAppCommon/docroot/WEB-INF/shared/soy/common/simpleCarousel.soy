{namespace common}

/**
 * @param items some path, list of items
 * @param itemType item variant
 * @param? title optional title string
 * @param? cmEventSource optional coremetrics event source
 * @param? mobWeb
 * @param? isIconCarousel
 * @param? moduleData
 */
{template .simpleCarousel autoescape="false"}
<div data-component="carousel" class="first">
	{if $title}<h2>{$title}</h2>{/if}
	<div data-component="carousel-mask" class="transactional">
		<ul data-component="carousel-list" data-carousel-page="0">
			{foreach $item in $items}
			{if $isIconCarousel}
				{call common.iconCarouselItem}
					{param item:$item /}
          {param moduleData:$moduleData /}
				{/call}
			{else}
				{delcall common.carouselItem variant="$itemType"}
					{param item:$item /}
					{param cmEventSource:$cmEventSource /}
					{param mobWeb:$mobWeb /}
          {param moduleData:$moduleData /}
				{/delcall}
			{/if}
			{/foreach}
		</ul>
	</div>
	<button data-component="carousel-prev" data-carousel-nav="prev">previous</button>
	<button data-component="carousel-next" data-carousel-nav="next"
	{if not $moduleData.hideViewAllButton}data-carousel-view-all="{not $moduleData.hideViewAllButton}"{/if}>next</button>
	{if not $moduleData.hideViewAllButton and ($moduleData.contentTitle or $moduleData.moduleTitle)}
		<div data-component="carousel-view-all">
			<a {if $moduleData.useViewAllPopup}data-view-all-popup data-moduleId={if $moduleData.moduleId}{$moduleData.moduleId}{/if}{else}href="{if $moduleData.viewAllButtonLink}{$moduleData.viewAllButtonLink}{/if}"{/if} cm-click="cmCreateElementTag,View All,Home Page-_-Carousel{if $moduleData.moduleId}-_-{$moduleData.moduleId}{/if}{if $moduleData.viewAllButtonLink}-_-{$moduleData.viewAllButtonLink}{/if}">view all
				<div class="offscreen">{if $moduleData.contentTitle}{$moduleData.contentTitle}{else}{$moduleData.moduleTitle}{/if}</div>
			</a>
		</div>
	{/if}
</div>
{/template}
