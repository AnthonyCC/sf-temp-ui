/* placeholder until rounded_corners.inc.js can be removed from code. */
function curvyCorners() {
	try {
		if (fd && fd.utils && fd.utils.isDeveloper()) {
			console.log('Script called curvyCorners, remove');
			console.trace();
		}
	} catch (e) {}
}