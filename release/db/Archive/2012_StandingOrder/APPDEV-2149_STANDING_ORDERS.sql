ALTER TABLE "CUST"."STANDING_ORDER" 
add "LOCK_ID" VARCHAR2(36 CHAR); 

ALTER TABLE "CUST"."STANDING_ORDER" 
add "LOCK_DATE" DATE; 

CREATE TABLE "CUST"."DUPLICATE_SOI_EMAIL_SENT"
	("CUSTOMER_ID" VARCHAR2(20 BYTE) NOT NULL ENABLE,
    "HASH"        VARCHAR2(50 BYTE) NOT NULL ENABLE,
    "SEND_DATE" DATE);

CREATE INDEX "CUST"."IDX_DUPL_SOI_EMAIL_CID" ON "CUST"."DUPLICATE_SOI_EMAIL_SENT" ( "CUSTOMER_ID");
