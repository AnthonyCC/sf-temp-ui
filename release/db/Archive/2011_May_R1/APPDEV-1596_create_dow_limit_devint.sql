CREATE TABLE CUST.DOW_LIMIT(
DAY_ID NUMBER CONSTRAINT DAY_ID_PK PRIMARY KEY,
LIMIT NUMBER(10,2) NOT NULL);

GRANT SELECT,INSERT,DELETE,UPDATE on CUST.DOW_LIMIT to FDSTORE_PRDA;

GRANT SELECT,INSERT,DELETE,UPDATE on CUST.DOW_LIMIT to FDSTORE_PRDB;