ALTER TABLE TRANSP.ASSET ADD ( BARCODE VARCHAR2(256 BYTE));

ALTER TABLE TRANSP.ASSET ADD ( CONSTRAINT ASSETBARCODE_UK UNIQUE (BARCODE));

CREATE TABLE TRANSP.ASSET_ACTIVITY
(
  ID             VARCHAR2(40 BYTE)              NOT NULL,
  DELIVERY_DATE  DATE			        NOT NULL,
  ASSET_NO       VARCHAR2(12 BYTE)              NOT NULL,
  EMPLOYEE_ID    VARCHAR2(40 BYTE)              NOT NULL,
  STATUS         VARCHAR2(10 BYTE)              NOT NULL,
  SCANNED_BY     VARCHAR2(55 BYTE),
  SCANNED_TIME   DATE
);

CREATE INDEX TRANSP.ASSETACTIVITY_EMP_IDX ON TRANSP.ASSET_ACTIVITY (EMPLOYEE_ID);

CREATE INDEX TRANSP.ASSETACTIVITY_IDX ON TRANSP.ASSET_ACTIVITY(ASSET_NO);

ALTER TABLE TRANSP.ASSET_ACTIVITY ADD (  PRIMARY KEY (ID) );

CREATE SEQUENCE TRANSP.ASSETACTIVITYSEQ
  START WITH 1
  MAXVALUE 9999999999999999
  MINVALUE 1
  NOCYCLE
  CACHE 20
  NOORDER;