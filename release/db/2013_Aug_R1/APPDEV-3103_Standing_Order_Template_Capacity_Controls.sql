ALTER TABLE DLV.GEO_RESTRICTION ADD (  APPLY_TO VARCHAR2(10 BYTE))

CREATE TABLE DLV.TIMESLOT_RESTRICTION
(
  DAY_OF_WEEK  VARCHAR2(10 BYTE),
  ZONE_CODE    VARCHAR2(10 BYTE),
  START_TIME   DATE,
  END_TIME     DATE,
  ACTIVE       VARCHAR2(1 BYTE),
  ID           VARCHAR2(10 BYTE),
  CONDITION    VARCHAR2(2 BYTE)                 NOT NULL
)
CREATE UNIQUE INDEX DLV.DOW_ZONE_ST_ET ON DLV.TIMESLOT_RESTRICTION
(DAY_OF_WEEK, ZONE_CODE, START_TIME, END_TIME)

ALTER TABLE DLV.TIMESLOT_RESTRICTION ADD (
  CONSTRAINT DOW_ZONE_ST_ET
 UNIQUE (DAY_OF_WEEK, ZONE_CODE, START_TIME, END_TIME))