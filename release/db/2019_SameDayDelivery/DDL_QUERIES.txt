ALTER TABLE DLV.TIMESLOT ADD  RELATIVE_CUTOFF_DAYS NUMBER(1) DEFAULT 1;

ALTER TABLE DLV.TIMESLOT_TEMPLATE ADD  RELATIVE_CUTOFF_DAYS NUMBER(1) DEFAULT 1;
ALTER TABLE DLV.TIMESLOT_TEMPLATE ADD IS_SHUTDOWN VARCHAR2(1);

CREATE TABLE TRANSP.SCRIB_EX
(
  ID          VARCHAR2(20 BYTE),
  SCRIB_ID    VARCHAR2(25 BYTE),
  STARTTIME   DATE                              NOT NULL,
  ENDTIME     DATE                              NOT NULL,
  CUTOFFTIME  DATE                              NOT NULL
) 
;


CREATE UNIQUE INDEX TRANSP.SCRIB_EX_PK ON TRANSP.SCRIB_EX
(ID);



ALTER TABLE TRANSP.SCRIB_EX ADD (
  CONSTRAINT SCRIB_EX_PK
 PRIMARY KEY
 (ID)) ;

ALTER TABLE TRANSP.SCRIB_EX ADD (
  FOREIGN KEY (SCRIB_ID) 
 REFERENCES TRANSP.SCRIB (SCRIB_ID));



ALTER TABLE TRANSP.PLAN ADD SCRIB_ID VARCHAR2(20);
