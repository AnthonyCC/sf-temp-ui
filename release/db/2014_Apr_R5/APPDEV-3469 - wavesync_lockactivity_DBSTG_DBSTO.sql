CREATE TABLE TRANSP.WAVE_INSTANCE_LOCKACTIVITY
(
  LOCK_ID               VARCHAR2(20 BYTE)         NOT NULL,
  INITIATOR             VARCHAR2(250 BYTE),
  LOCK_DATETIME         DATE,
  LOCK_RELEASEDBY       VARCHAR2(250 BYTE),
  RELEASELOCK_DATETIME  DATE
);

ALTER TABLE TRANSP.WAVE_INSTANCE_LOCKACTIVITY ADD ( PRIMARY KEY (LOCK_ID));

CREATE UNIQUE INDEX TRANSP.WAVEINSTANCE_LOCKACTY_IDX ON TRANSP.WAVE_INSTANCE_LOCKACTIVITY (RELEASELOCK_DATETIME);
